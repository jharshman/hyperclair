language: go
go:
- 1.5

install:
- go get -d -v ./...
- go get github.com/mitchellh/gox
- gox -os="linux" -os="darwin" -arch="amd64 386" -output="out/{{.Dir}}-{{.OS}}-{{.Arch}}" -ldflags "-X github.com/jharshman/hyperclair/cmd.version=$(cat VERSION)"

